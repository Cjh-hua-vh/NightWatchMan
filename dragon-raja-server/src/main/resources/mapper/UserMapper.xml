<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dragonraja.forum.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="UserResultMap" type="com.dragonraja.forum.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="bloodline_grade" property="bloodlineGrade"/>
        <result column="faction" property="faction"/>
        <result column="status" property="status"/>
        <result column="online_status" property="onlineStatus"/>
        <result column="role" property="role"/>
        <result column="signature" property="signature"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT id, username, password, nickname, avatar, bloodline_grade,
               faction, status, online_status, role, signature, create_time, update_time
        FROM user
        WHERE username = #{username}
    </select>

</mapper>
